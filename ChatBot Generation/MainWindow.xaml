<Window x:Class="ChatBot_Generation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ASCII Art Header -->
        <Border Grid.Row="0" Background="#2D2D2D" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <TextBlock x:Name="txtAscii" 
                       Text="Loading..." 
                       FontFamily="Consolas" 
                       FontSize="10" 
                       Foreground="#00AA00" 
                       HorizontalAlignment="Center"
                       TextAlignment="Center"/>
        </Border>

        <!-- Main Content with Tabs -->
        <TabControl Grid.Row="1" Background="White" FontSize="14">

            <!-- CHAT TAB -->
            <TabItem Header="Chat">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
                        <TextBox x:Name="txtChat" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="Transparent" Foreground="White" FontFamily="Consolas" FontSize="13" Padding="10" BorderThickness="0"/>
                    </Border>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtMessage" Grid.Column="0" Height="40" Background="White" Foreground="Black" FontSize="14" Padding="10" BorderBrush="#CCCCCC" BorderThickness="1" KeyDown="txtMessage_KeyDown" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="Send" Width="100" Height="40" Margin="10,0,0,0" Background="#1976D2" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand" Click="SendButton_Click"/>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- TASKS TAB -->
            <TabItem Header="Tasks">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#E3F2FD" Padding="15" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Add New Task" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBox x:Name="txtTaskInput" Height="35" Background="White" Padding="10" BorderBrush="#CCCCCC" Margin="0,0,0,10"/>
                            <Button Content="Add Task" Height="35" Background="#27AE60" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="AddTask_Click"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5">
                        <ListBox x:Name="lstTasks" Background="Transparent" Foreground="Black" BorderThickness="0" Padding="10">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F5F5F5" Padding="10" Margin="0,0,0,5" CornerRadius="3">
                                        <StackPanel>
                                            <CheckBox Content="{Binding Description}" Foreground="Black" IsChecked="{Binding IsCompleted}" FontSize="13"/>
                                            <TextBlock Text="{Binding Created, StringFormat='Created: {0:MMM dd, yyyy HH:mm}'}" Foreground="#666666" FontSize="11"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </TabItem>

            <!-- QUIZ TAB -->
            <TabItem Header="Quiz">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#FFF3E0" Padding="15" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock x:Name="txtQuizTitle" Text="Cybersecurity Quiz" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock x:Name="txtQuizProgress" Text="Question 1 of 10" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="20">
                        <StackPanel x:Name="pnlQuizContent">
                            <TextBlock x:Name="txtQuestion" Text="Loading question..." FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,20"/>
                            <RadioButton x:Name="optA" GroupName="QuizOptions" Content="A) " FontSize="14" Padding="10" Margin="0,5" Background="#F5F5F5" Foreground="Black"/>
                            <RadioButton x:Name="optB" GroupName="QuizOptions" Content="B) " FontSize="14" Padding="10" Margin="0,5" Background="#F5F5F5" Foreground="Black"/>
                            <RadioButton x:Name="optC" GroupName="QuizOptions" Content="C) " FontSize="14" Padding="10" Margin="0,5" Background="#F5F5F5" Foreground="Black"/>
                            <RadioButton x:Name="optD" GroupName="QuizOptions" Content="D) " FontSize="14" Padding="10" Margin="0,5" Background="#F5F5F5" Foreground="Black"/>
                            <TextBlock x:Name="txtQuizFeedback" TextWrapping="Wrap" FontSize="14" Margin="0,20,0,0" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="btnStartQuiz" Content="Start Quiz" Width="150" Height="40" Background="#3498DB" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand" Click="StartQuiz_Click"/>
                        <Button x:Name="btnSubmitAnswer" Content="Submit Answer" Width="150" Height="40" Background="#27AE60" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand" Visibility="Collapsed" Click="SubmitAnswer_Click" Margin="10,0,0,0"/>
                        <Button x:Name="btnNextQuestion" Content="Next" Width="120" Height="40" Background="#E67E22" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand" Visibility="Collapsed" Click="NextQuestion_Click" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- ACTIVITY LOG TAB -->
            <TabItem Header="Activity Log">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Recent Activity" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Refresh" Width="100" Height="30" Background="#9B59B6" Foreground="White" Cursor="Hand" Click="RefreshLog_Click"/>
                    </Grid>
                    <Border Grid.Row="1" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5">
                        <ListBox x:Name="lstActivityLog" Background="Transparent" Foreground="Black" BorderThickness="0" Padding="10">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F5F5F5" Padding="10" Margin="0,0,0,5" CornerRadius="3">
                                        <StackPanel>
                                            <TextBlock Foreground="#006600" FontSize="12" FontWeight="Bold">
                                                <Run Text="["/><Run Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"/><Run Text="]"/>
                                                <Run Text="{Binding Action}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Details}" Foreground="#333333" FontSize="13" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>